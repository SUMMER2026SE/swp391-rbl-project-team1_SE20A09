package com.sportvenue.entity.enums;

/**
 * Types of chat messages supported by the system.
 */
public enum MessageType {
    /** Normal text message. */
    TEXT,
    /** Image/file attachment. */
    IMAGE,
    /** System-generated message (e.g., "User joined"). */
    SYSTEM
}
